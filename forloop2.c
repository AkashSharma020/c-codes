#include<stdio.h>
int main()
{
    int n,sum=0;
    printf("Enter the value of N = ");
    scanf("%d",&n);
    for(int i=1; i<=n; i=i+1)
    {
        sum=sum+i;
        square=square*n
        printf("%d",square);
    
    }
    return 0;
}